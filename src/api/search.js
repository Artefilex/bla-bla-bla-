
import axios from "axios";

// export const searchBooks = async (searchText, searchBy) => {
//   const response = await  axios.get(`http://localhost:3001/books/?title=${searchText}`)
//   if (response.data && response.data.results) {
//     return response.data.results;
//   }
// };

